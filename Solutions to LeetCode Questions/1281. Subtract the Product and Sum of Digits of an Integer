class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        number_word = str(n)
        product= 1
        sum= 0
        for i in range(0, len(number_word)):
            product= product * int(number_word[i])
            sum+=int(number_word[i])
        return product - sum
