def numIdenticalPairs(nums):
        import random
        no_of_pairs = 0
        while True:
            pair = random.sample(nums, 2)
            if pair[0]<=pair[1]:
                no_of_pairs +=1
        return no_of_pairs
 #results in an infinite loop, still working on it.
 
 
